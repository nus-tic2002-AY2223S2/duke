package duke.command;

import duke.type.CommandType;

public class AddCommand extends Command {
    public AddCommand(CommandType commandType, String command) {
        super(commandType, command);
    }

}
